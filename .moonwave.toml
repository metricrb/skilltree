# CLI: `moonwave build --out-dir site` (default: build/). Guides source: `docs/*.md` in this repo.

[project]
name = "SkillTree"
description = "A declarative, reusable skill tree system for Roblox games"
gitRepoUrl = "https://github.com/metricrb/skilltree"

gitSourceBranch = "main"

[changelog]
enabled = true

[docusaurus]
url = "https://metricrb.github.io"
baseUrl = "/skilltree/"
tagline = "Declarative skill trees, server authority, multi-currency — strict Luau."

[navbar]
title = "SkillTree"
